# Per-deployment secrets and generated state -- never commit.
# The repo is public; keys/ holds the FABRIC sliver and bastion PRIVATE keys.
keys/

# Generated by gen_inventory.py for one specific slice (management IPs,
# MACs, per-node assignments). Regenerate rather than commit.
plan/

# Runtime state and per-node logs from a setup run.
state/
logs/
